const QuestionText = ({ question }) => {
  return (
    <div className="block text-2xl text-gray-900 dark:text-white mb-10 text-center">
      {question}
    </div>
  );
};

export default QuestionText;
