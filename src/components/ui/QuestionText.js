const QuestionText = ({ question }) => {
  return (
    <div className="block text-lg md:text-3xl text-gray-900 dark:text-white mb-1 text-center">
      {question}
    </div>
  );
};

export default QuestionText;
